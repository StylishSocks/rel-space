export const API_BASE_URL = "http://localhost:5000/api";

export const ENDPOINTS = {
  parsed_model: `${API_BASE_URL}/cad/result`, // Endpoint for the model JSON file
  sensors: `${API_BASE_URL}/sensors`, // Endpoint for sensor data
  // Add other endpoints as needed
};
